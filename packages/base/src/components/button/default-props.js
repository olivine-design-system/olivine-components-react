import { TYPE, SHAPE, SIZE } from './constants.js'

export const defaultProps = {
  disabled: false,
  isLoading: false,
  isSelected: false,
  type: TYPE.primary,
  overrides: {},
  shape: SHAPE.default,
  size: SIZE.default,
}
